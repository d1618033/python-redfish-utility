language: python
python:
  - "2.7"
before_install:
  - python --version
  - pip install -U pip
  - python setup.py install
  - pip install -U pytest
  - pip install codecov
# command to run tests
script: nosetests
